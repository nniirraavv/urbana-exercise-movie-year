import * as moviesData from "./data";

/*const query = {
    movies: async ({limit}, context) => {
        return limit ? moviesData.slice(0, limit) : moviesData;
    },
    movie: async ({title}, context) => {
        return moviesData.find(movie => movie.title === title);
    }
};

module.exports = query;*/